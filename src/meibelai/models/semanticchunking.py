"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from meibelai.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class SemanticChunkingTypedDict(TypedDict):
    r"""SemanticChunking"""

    include_metadata: bool
    include_prev_next_rel: bool
    buffer_size: NotRequired[Nullable[int]]
    breakpoint_percentile_threshold: NotRequired[Nullable[int]]


class SemanticChunking(BaseModel):
    r"""SemanticChunking"""

    include_metadata: bool

    include_prev_next_rel: bool

    buffer_size: OptionalNullable[int] = UNSET

    breakpoint_percentile_threshold: OptionalNullable[int] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["buffer_size", "breakpoint_percentile_threshold"])
        nullable_fields = set(["buffer_size", "breakpoint_percentile_threshold"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
